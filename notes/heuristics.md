"Use the data structure that enforces the invariant, instead of enforcing it through logic"

"When a metric output looks straightforward, ask 'what would have to be true mathematically for this exact value to occur?' Often the math constrains the failure mode more sharply than the description does."

"Substring matching evaluates if the answer is literally in retrieved text. Semantic (LLM-judged) evaluation evaluates if the answer is derivable from retrieved text. The right tool depends on what you're measuring."

